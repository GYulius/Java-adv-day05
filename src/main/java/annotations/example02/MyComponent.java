package annotations.example02;

// Apply the ComponentInfo annotation to the MyComponent class with specified values
@ComponentInfo(name = "MyComponent", date = "2024-06-20", currentVersion = 1)
public class MyComponent {
    // The MyComponent class body can contain fields, methods, and other components
}